---
application: core-app
module: State
title: "GroLabs — System state"
status: Draft
audience: "Contributors updating state docs at the end of a PR, and anyone (including web/desktop Claude) bringing a fresh conversation up to speed on GroLabs."
scope: "Index + update protocol for the three state docs (modules.md, schema.md, in-flight.md). Not feature content itself — it governs how the snapshot files are kept current and how they're pasted for context."
actors:
  - name: Contributor
    type: human
    definition: "Engineer who, at the end of each PR, updates the relevant state doc (module change → modules.md, schema change → schema.md, work change → in-flight.md) and bumps the Generated date."
  - name: Web/desktop Claude
    type: system
    definition: "An AI assistant without file access; brought up to speed by pasting the relevant state-doc sections at the start of a conversation."
rules:
  - id: R-1
    statement: "State docs are updated at the end of every PR that changes module state, schema, or open work, and the Generated YYYY-MM-DD line of any touched file is bumped."
    truth: true
    rationale: "Intro and update protocol step 4."
  - id: R-2
    statement: "When schema changes, schema.md is regenerated by re-querying the affected tables from the live DB, not transcribed from the migration, because the live DB is the source of truth."
    truth: true
    rationale: "Update protocol step 2."
  - id: R-3
    statement: "in-flight.md tracks PR lifecycle: PR opened → add to Open PRs; PR merged → remove from Open PRs, mark its branch deletable, and remove debt items it resolved; new TODO/FIXME → add to the code-level list."
    truth: true
    rationale: "Update protocol step 3."
  - id: R-4
    statement: "modules.md alone is usually enough for feature-scoping conversations; add schema.md when DB changes are involved and in-flight.md when the question touches current work."
    truth: true
    rationale: "'Reading this for context' section."
useCases:
  - id: T-1
    title: "Keep state docs current after a PR"
    given: "A PR that added a route and ran a migration is about to merge"
    when: "The contributor follows the update protocol"
    then: "modules.md gets the new route, schema.md is re-queried from the live DB for the changed tables, in-flight.md is updated for the PR/branch/debt, and each touched file's Generated date is bumped"
    verifies: [R-1, R-2, R-3]
  - id: T-2
    title: "Hand off context to a file-less assistant"
    given: "Web/desktop Claude has no file access"
    when: "The contributor pastes modules.md (plus schema.md/in-flight.md as the topic requires) at the start of the conversation"
    then: "The assistant has enough current-state context to scope the feature work"
    verifies: [R-4]
---

# GroLabs — System state

These documents describe GroLabs's current shape. They are updated at the
end of every PR that changes module state, schema, or open work.

- **modules.md** — what each module does, its routes, its actions, its
  gaps. Includes a deeper "Catalog → Products" sub-section covering
  variants and the manufacturer field.
- **schema.md** — every table, its columns, its relationships, its RLS
  shape. Generated from `information_schema`, `pg_policies`,
  `pg_constraint`, `pg_indexes`, and `pg_trigger`.
- **instances.md** — the canonical tenant/instance row map in the
  production DB (template 0, HPC 11, test storefront 12, synthetic
  99999, the deleted-Wazú graveyard). Update in the same PR as any
  tenant/instance row change.
- **in-flight.md** — open PRs, active branches, known issues and debt.

## Update protocol

At the end of each PR:
1. If module state changed (route added, server action added/removed,
   read-only → CRUD, etc.): update the relevant section of
   **modules.md**.
2. If schema changed (migration ran, new table/column/policy/trigger):
   update **schema.md** — re-query the affected tables from the live
   DB rather than transcribing from the migration, since the live DB is
   the source of truth.
3. Add or remove items from **in-flight.md**:
   - PR opened → add to "Open PRs" table.
   - PR merged → remove from "Open PRs"; mark the branch as "merged,
     can be deleted" in "Active branches"; remove any debt items the PR
     resolved.
   - New TODO/FIXME left in code → add to "Code-level (TODO comments)".
4. Bump the "Generated YYYY-MM-DD" line at the top of any file you
   touched.

## Reading this for context

Web/desktop Claude does not have file access. To bring it up to speed
on GroLabs, paste the relevant sections of these files at the start of a
conversation. **modules.md** alone is usually enough for feature-scoping
conversations; add **schema.md** when database changes are involved;
add **in-flight.md** when the question touches what's currently being
worked on.

A typical context-handoff prompt looks like:

> I'm working on GroLabs. Current state: [paste modules.md]. Schema for
> the relevant domain: [paste the schema.md sections that matter]. The
> work in flight: [paste the relevant rows of in-flight.md].
>
> Question / task: …

For full repo context, also include **CLAUDE.md** (conventions) and the
relevant `docs/funnel/spec.md` / `docs/funnel/prototype.tsx.reference`
when the funnel is in scope.
