/**
 * Scorer barrel — importing this module runs the per-category `register(...)`
 * side effects, populating the registry. Import order is irrelevant (every
 * code is distinct). Add a new category file here when one is introduced.
 */

import "./seo";
import "./aeo";
import "./perf";
import "./search";
import "./pdp";
import "./auth";
