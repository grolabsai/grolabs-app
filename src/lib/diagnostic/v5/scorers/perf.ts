/**
 * Page-performance category scorers (category_code = "page_performance",
 * stage = discovery).
 *
 * TODO(Prompt 5): replace each `notImplemented` stub with a real scorer.
 * Primary evidence: PSI (Google PageSpeed Insights — Core Web Vitals).
 */

import { notImplemented, register } from "../registry";

register("perf.cwv.lcp", notImplemented);
register("perf.cwv.inp", notImplemented);
register("perf.cwv.cls", notImplemented);
